# Review — graphify 계열 (#169 / #170 / #173)

- **Date**: 2026-09-22
- **대상**: PR 대상 diff — `install.sh`, `scripts/wikihub_graphify.sh`, `wikihub.yaml.example`, `README.md`, `_system/commands/lint.md`
- **리뷰 diff 규모**: 91줄 (5 파일)

## 모델 실측 비교 (동일 실제 diff)

`kimi-k3` 를 쓰던 리뷰가 **420s 타임아웃**으로 실패했고, 비용 문제가 제기되어
후보 3종을 **동일 조건**(실제 작업 diff, 동일 프롬프트 6.6KB)으로 측정했습니다.

| 모델 | 소요 | 검출 | **실재 결함** |
|---|---|---|---|
| **`kimi-k2.7-code`** | **36s** | 5건 | **3건** |
| `deepseek-v4-pro` | 46s | 3건 | 2건 |
| `nemotron-3-ultra` | 29s | 2건 | 1건 |
| `kimi-k3` (기존) | 420s+ | — | 타임아웃 (결과 없음) |

**`kimi-k2.7-code` 채택** — 최단 시간에 실재 결함을 최다 검출.

### 주의 — 이전 측정과 상충

#197 당시 bash 스크립트 fixture(결함 10개)에서는 `kimi-k3` 10건 / `kimi-k2.7-code` 3건으로
**정반대** 결과였습니다. 실제 리뷰 대상(markdown + install.sh + yaml)에서는 `kimi-k2.7-code`
가 우수합니다. **fixture 종류에 따라 검출력이 역전되므로 실측 대상으로 판단해야 합니다.**

## 검출된 실재 결함 (전부 실측 확인 후 수정)

### ① `lint.md` — `god-nodes` 를 고아 탐지로 오표기 (kimi-k2.7-code)

3차 계층 설명에 "고아 페이지(degree=0) 탐지 → `graphify god-nodes`" 라고 썼으나,
실측 결과 정반대입니다:

```
$ graphify god-nodes --graph graph.json
God nodes (most connected):
  1. 2026년에 매일 어떤 소프트웨어를 사용하시나요? ... - 67 edges
  2. SK하이닉스 - 13 edges
```

`god-nodes` = **가장 연결이 많은 노드(허브)**. degree=0 탐지에 쓰면 **정반대 결과**.

**출처는 #173 이슈 본문**입니다 — "고아 페이지 탐지는 query로 부적합 — 별도 Python helper
또는 god-nodes CLI 검토". 이슈 본문의 미검증 제안을 그대로 옮긴 것이 원인입니다.
(#191에서 이슈 본문 수치가 틀렸던 것과 같은 패턴.)

**수정**: `god-nodes` 를 명시적 경고로 바꾸고, 고아 탐지는 helper 전용임을 못박음.

### ② `lint.md` — `$WIKIHUB_VENV` 가용성 (kimi-k2.7-code, [mid])

**[오지적 판정]** — 리뷰는 "`$WIKIHUB_VENV` 가 export 안 될 수 있다"고 했고,
저도 처음엔 systemd unit 에 `WIKIHUB_VENV` 가 없어(Environment 는 `PATH`/`WIKIHUB_YAML`/
`WIKIHUB_SRC`/`EnvironmentFile` 만) 정정했다가, **`install.sh:1351` 이
`export WIKIHUB_VENV="$VENV_PATH"` 를 `session-env.sh` 에 bake 하는 것을 확인하고 revert** 했습니다.

```
install.sh:1348-1354   session-env.sh 생성 — WIKIHUB_HOME/YAML/VENV + PATH guard
install.sh:1377+       Hermes terminal.shell_init_files 등록 (issue #186)
lint.md:205            기존 코드가 이미 $WIKIHUB_VENV/bin/python3 사용 (동일 커밋 6af6898)
```

즉 `$WIKIHUB_VENV` 는 **repo 규약**이고 `lint.md:205` 가 선례입니다. 제가 unit 만 보고
단정한 것이 오류였습니다.

**단, 배포 지연은 실재합니다** — 운영 배포본(HEAD `9656760`, VERSION 0.1.16)에는 #186 코드가
0건이고 `~/.config/wikihub/session-env.sh` 도 없습니다. `shell_init_files: []` 상태.
**운영 소관(jisaseo)** 이며, 본 PR 은 repo 규약 정합만 담당합니다.

**수정**: `$WIKIHUB_VENV` 유지 + session-env.sh 주입 경로를 주석으로 명시.

### ③ `install.sh` — pin 하한 주석 논리 불일치 (deepseek-v4-pro, nemotron-3-ultra 동시 지적)

주석이 "0.9.21 이 OLLAMA_HOST auto-detection 을 추가해" 라면서 하한은 `0.9.20` 이라
**근거와 값이 불일치**했습니다. (#170 이슈 본문의 "0.9.21(OLLAMA_HOST 자동감지) 이후 안정성 확보"
표현을 그대로 옮긴 결과.)

**수정**: 하한은 0.9.20 유지(0.9.x 진입이 목적), 0.9.21 언급은 참고로 격하.

### ④ `README.md` — graphify URL 오류 (kimi-k2.7-code)

**2건 모두 오류였습니다.**

```
216행 (기존)  https://github.com/safishamsi/graphify        ← 오류
227행 (신규)  https://github.com/Graphify-Labs/graphify     ← 정확

PyPI graphifyy 0.9.65 메타데이터:
  Homepage   : https://github.com/Graphify-Labs/graphify
  Repository : https://github.com/Graphify-Labs/graphify
  License    : Apache-2.0
```

opencode 가 신규 작성 시 `safishamsi` 를 생성했고(제가 URL 을 주지 않음),
기존 216행도 같은 오류였습니다. **PyPI 정본으로 둘 다 통일** — `safishamsi` 0건.

### ⑤ `wikihub_graphify.sh:68` — 메시지 패키지명 오류 (deepseek-v4-pro)

바로 위 줄은 `graphifyy` 로 정정했는데 아래 `_write_graphify_failure` 힌트는
`pip install graphify`(패키지명 아님)로 남아 **같은 오류 경로에서 불일치**.

**수정**: `pip install graphifyy` 로 통일.

## 기각 / 보류

- **[low] "~1-2k tokens 비용 추정 근거 미측정"** — #173 이슈 §5 에 실증 기록 있음
  (`graphify query` 실증 46개 노드 33개 엣지 2000 token 이하, 2026-07-30).
  본 PR 에서는 수치 보존. graph.json 실측은 1.28MB / 2742 노드 / 770 엣지로
  전체 직렬화 ≈26만 token 추정 — 1차 계층의 회피 대상이 실재함.
- **[low] `graphify_max_version: 0.99.99` vs `<1.0.0` 비동등** — 코드가 읽지 않는
  문서 전용 값(실측 사용 0건). `wikihub.yaml.example` 주석이 그 사실을 명시.

## 검증 (직접 실행)

```
bash -n install.sh                              OK
bash -n scripts/wikihub_graphify.sh             OK
grep -c 0.8.0 (3 파일)                          0 / 0 / 0
grep -c 0.9.20 (3 파일)                         2 / 1 / 1
grep -c safishamsi README.md                    0
pip install "graphifyy>=0.9.20,<1.0.0"          Requirement already satisfied

lint unit PATH 환경에서 실제 실행:
  graphify query 'OpenCode' --graph .../graph.json
  → Traversal: BFS depth=2 | Start: ['OpenCode'] | 6 nodes found
  → NODE/EDGE 출력 정상

graphify god-nodes --graph .../graph.json
  → God nodes (most connected): ... - 67 edges    ← ① 오표기 근거

python3 -m pytest tests/ -q                      93 passed, 1 skipped
```

## 결론

**PASS** — 실재 결함 5건 전량 수정. ②는 오지적으로 판정했으나 배포 지연이라는 실재
이슈를 드러냈습니다(운영 소관).
