# Review — issue #176 (quality)

- **일시**: 2026-09-14 (KST)
- **대상**: `scripts/_helpers/detect_alias_duplicates.py` (distinct-path 필터)
- **리뷰 실행**: `opencode run` (primary `ollama-cloud/deepseek-v4-flash`) → `review-quality` subagent
- **Verdict**: PASS — [high]/[mid] 0건, [low] 2건

## 지적

| 등급 | 지적 | 조치 |
|---|---|---|
| [low] | 주석의 `DISTINCT` 표현이 의도를 흐림 | 주석 재작성 (alias variant vs duplicate 설명 명시) |
| [low] | `len(pages) < 2` 와 신규 필터의 순서 관계 설명 부족 | 주석에 근거 보강 |

## 영역별

- **보안**: 이슈 없음 (`relative_to()`, eval/shell 없음)
- **성능**: alias group 당 O(n), 저렴한 가드 이후 — hot-path 아님
- **유지보수성**: 기존 `cats` 패턴과 일관, category 분기 이전 배치
- **의존성**: 신규 없음
- **엣지 케이스**: 적절히 처리

## 모델 라우팅 주의

`--print-logs` 실측 결과 review-quality subagent 는 `minimax-m2.7` 로 실행됨.
`.opencode/agent/review-quality.md` frontmatter 의 `model` 이
`opencode.json` 의 `agent.review-quality.model` 을 덮어쓰기 때문 — 별도 이슈 대상.
