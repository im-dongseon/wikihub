# Review 2 — Quality (issue #185)

- **Date**: 2026-09-21
- **Subagent**: `review-quality` (`mode: subagent`) — **정상 호출됨**
- **Actual model**: `ollama-cloud/qwen3.5:397b` (`agent=review-quality mode=subagent modelID=qwen3.5:397b`)
- **Primary**: `ollama-cloud/deepseek-v4-flash`
- **Scope**: 동일 + `tests/test_config.py`

## Verdict: NEEDS CHANGES → 최PM 실증 후 부분 수정

## Findings 실증 판정

| # | 등급 | 지적 | 실증 | 판정 |
|---|---|---|---|---|
| 1 | high | `config.py:189` `WIKIHUB_YAML` 경로 검증 부재 (traversal) | v0.1.16 원본도 `Path(env_path)` 그대로 사용. 본 diff 가 도입한 위험 아님. env 는 운영자 제어 값이며 코드 실행 경로가 아님 | **기각** (범위 밖) |
| 2 | mid | `expanduser` 불일치 — `WIKIHUB_YAML` 에만 없음 | 실증: `Path("~/x")` 는 `~` literal → 존재검사 실패. 운영은 절대경로 주입이라 무영향이나 일관성 결함 | **수정** (`Path(env_path).expanduser()`) |
| 3 | mid | 에러 메시지가 전체 경로 노출 | v0.1.16 원본도 `reason=f"wikihub.yaml 없음: {path}"`. 진단에 필요한 정보 | **기각** (기존 동작) |
| 4 | low | docstring 에 issue #185 참조 — drift 위험 | resolution 순서는 운영자가 이해해야 할 계약 | **미변경** (유지 판단) |
| 5 | low | 테스트 fixture `_minimum_yaml()` 이 `/opt/wikihub` 사용 | 타당하나 **기존 12건 테스트가 공유하는 fixture** — 변경 시 전체 영향, 본 diff 범위 밖 | **보류** |
| 6 | low | 신규 테스트가 resolution 경로를 단언하지 않음 | 3번 테스트가 `VaultSyncFatal.reason` 으로 경로 단언 + `/opt/wikihub` 부재 회귀 가드 | **충족** (부분 기각) |

## 수정 후 검증

- `Path(env_path).expanduser()` 적용 → `WIKIHUB_YAML='~/x'` 케이스 해소 (실측: `resolved = <HOME>/wikihub.yaml | 존재 = True`)
- `pytest tests/test_config.py` → **15 passed**
- 회귀: 운영 `wikihub.yaml` 정상 파싱 유지
