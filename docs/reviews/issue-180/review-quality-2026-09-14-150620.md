# Review — issue #180 (quality)

- **일시**: 2026-09-14 (KST)
- **대상**: `_system/commands/lint.md`, `_system/commands/ingest.md`
- **리뷰 실행**: `opencode run` (primary `ollama-cloud/deepseek-v4-flash`) → `review-quality` subagent
- **Verdict**: Approve — [mid] 0건, [low] 3건

## 영역별

- **보안**: 이슈 없음
- **일관성/모순**: 핵심 아키텍처 주장(`Type=oneshot` 1차, flock 2차, fd 상속 실패 모드)이 두 파일의 Step 0 / `## 동시성` / Step 0.5 에서 동일하고 일관됨. 모순 표현 없음

## [mid] 오탐 정정

subagent 가 "lint.md:383 에 `.wi-<vault_id>.lock` 이 잘못 들어갔다"고 [mid] 로 보고했으나, **오탐**이다. 해당 라인은 `ingest.md` 의 `## 동시성` 섹션이며 vault 별 lock 이므로 정상. lint.md 의 동시성 섹션은 `.wl.lock` 을 올바르게 쓴다 (working tree 대조 확인). → 수정 불필요.

## [low] 반영

1. `ingest.md` scope drift — systemd 경유 경로(timer·수동)는 1차 가드 담당, 2차 flock 은 Hermes 직접 호출 경로 대상임을 명시하는 연결 문장 추가
2. lint.md `"Bad fd"` 예시 — 해당 shell 에 fd 200 미할당임을 명확화
3. (선택) lint.md → ingest.md 역방향 cross-ref — 미반영 (양방향 참조는 과잉)

## 모델 라우팅 주의

`--print-logs` 실측: `minimax-m2.7` 로 실행됨 (`.opencode/agent/review-quality.md` frontmatter 우선).
