# Review 2 — Quality (issue #190)

- **Date**: 2026-09-21
- **Subagent**: `review-quality` 요청 (프롬프트에 subagent 명시)
- **Actual execution**: `agent=build mode=primary` — **primary 직접 리뷰** (subagent 미호출)
  - ⚠️ 정직 기록: correctness 리뷰와 동일 사유. 모델은 `deepseek-v4-flash`.
- **Scope**: 동일 3 파일

## Verdict: APPROVE WITH CHANGES → 수정 후 PASS

## Findings → 최PM 처리

| # | 등급 | 내용 | 처리 |
|---|---|---|---|
| 1 | mid | `${HOME%/}` 정규화 불일치 — `_hermes_root()` fallback(`903`), `_hermes_config_path()` fallback(`939`), `_hermes_stray_candidates()` 후보 b(`959`) 가 raw `$HOME` 사용 | **수정** — 3곳 모두 `${HOME%/}` 적용 |
| 2 | mid | `_migrate_hermes_stray_config()` 가 `_hermes_stray_candidates()` 를 3회 호출 → `_hermes_agent_profile` subprocess 중복 실행 + TOCTOU | **수정** — 후보를 1회 계산해 배열 재사용 |
| 3 | low | `_dirs` O(n²) dedup | 미변경 (n≤2, 무해) |

## Finding 1 실증 (지적이 실측으로 확인됨)

```
HOME=/home/ubuntu   → candidates = .../profiles/jisaseo/home/.hermes/config.yaml
HOME=/home/ubuntu/  → candidates = .../profiles/jisaseo/home/.hermes/config.yaml
                                   /home/ubuntu//.hermes/config.yaml      ← 이중 슬래시
```

이중 슬래시로 default 정본과 **문자열 비교가 어긋나** 가드가 무력화될 수 있었습니다 (후보 b 가 default 정본과 다른 문자열이 됨). 수정 후 두 케이스 모두 후보 1건으로 동일, default 정본 포함 **0건**.

## subagent 주장 중 DOWNGRADE 판정 (리뷰어 자체 triage)

- [claimed high] 가드 우회 — 이론적. 후보는 모두 내부 생성(HOME/HERMES_HOME/agent.profile)이며 공격자 제어 경로 없음. 가드는 3중으로 defense-in-depth. **기각**
- [claimed mid] `set -e` abort — 실제 버그 아님 (guard 라인 면제 + `|| true`). **기각**
- [claimed mid] dedup 로직 버그 — trace 결과 정확. **기각**

## 미적용 (기존 코드)
- `_verify_hermes_skill_registration()` 무변경 (본 diff 범위 밖)
- `_patch_hermes_external_dirs()` 로직·marker·백업 관례 무변경
