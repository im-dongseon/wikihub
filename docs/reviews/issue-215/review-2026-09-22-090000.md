# Review — referenced_by 매칭 규정 3중 분기 (issue #215)

- **Date**: 2026-09-22
- **대상**: `scripts/_helpers/ref_match.py`(신규) · `ref_ledger.py`(신규) · `ref_audit.py`(신규) · `tests/test_ref_match.py`(신규) · `_system/commands/ingest.md` · `_system/commands/lint.md`
- **diff 규모**: 659줄

## 모델

| 단계 | 모델 | 비고 |
|---|---|---|
| 구현 | (최PM 직접) | opencode `glm-5.3` 가 1,500s timeout 안에 파일 생성 미완 (research 단계 소진). 판정 함수는 사전 프로토타입으로 검증된 상태였음 |
| 리뷰 | `ollama-cloud/kimi-k2.7-code` | 185s |

## 판정

**PASS** — high 1 / mid 1 / low 3 (전량 수정)

리뷰가 테스트를 **직접 실행**(33 passed)하고 지정한 8개 리스크를 하나씩 검증한 뒤 판정했습니다.

## [high] — 제 re.I 회귀 테스트가 **vacuous** 였다 (실재 결함)

리뷰가 찾아낸 가장 중요한 결함입니다.

```
제 테스트:  assert R.matches("Go", "go") is True

문제: ascii_pattern("Go") → (?<![a-z0-9])go(?![a-z0-9])
      이름을 소문자로 낮추므로 본문 "go" 는 **re.I 없이도 매칭**된다
      → re.I 를 제거해도 테스트가 통과 = 아무것도 증명하지 못함
```

**실측 확인:**

```
      re.I=True  re.I 없음
'go'      True       True     ← 제 테스트가 쓰던 케이스 (vacuous)
'GO'      True       False    ← 진짜 회귀 시나리오
'Go'      True       False
'GITHUB CLI' True    False
```

**수정**: 비-vacuous 케이스(`GO`·`GitHub CLI` 대문자) 6건을 parametrize 로 추가하고,
`test_re_i_absence_is_detectable` 로 회귀 검출력 자체를 단언했습니다.

**회귀 검출력 실증** (re.I 를 실제로 제거해 측정):

```
수정 전 테스트:  re.I 제거 → 1건 실패   (사실상 검출 실패)
수정 후 테스트:  re.I 제거 → 15건 실패  ✅
                 원복      → 40 passed
```

## [mid] `_match_line()` 이 분기 로직을 중복 구현 — 수정

`matches()` 와 같은 패턴을 다시 만들고 분기를 재구현해, 한쪽만 고치면 결과가 갈라질
위험이 있었습니다 (모듈 docstring 의 "단일 정본" 목표 위반).

**수정**: `ascii_pattern()` 단일 정본을 공유하도록 정리하고, `# ⚠️ re.I 필수 (matches 와 동일)`
주석으로 동기화 지점을 명시했습니다.

## [low] 3건 — 판정 및 반영

| 지적 | 판정 |
|---|---|
| 줄 번호가 body 기준(파일 절대 아님) | **수용** — docstring 에 "body 기준" 명시. ledger 소비자는 body 기준을 알 필요가 있음 |
| audit/ledger 가 traversal boilerplate 중복 | **수용(보류)** — 매칭 로직 중복은 없고(둘 다 `R.verdict()` 호출), 순회 boilerplate 만 중복. 별도 리팩토링은 범위 밖 |
| malformed frontmatter·비문자열 ref 무가시적 skip | **수용(보류)** — 방어적 처리는 정상. 가시성 추가는 별도 개선 |

## 리뷰가 "No substantive findings" 로 확인한 것

1. **re.I 적용** — `matches()`:104, `_match_line()`:145 양쪽 확인
2. **분기 우선순위** — `classify()` 가 ext 를 먼저 검사 (`_lint_step6.py` → ext)
3. **P3 우선순위** — `verdict()` 가 매칭 전에 `HOMOGRAPH_NAMES` 검사. `Go` 페이지는 본문에
   유효한 `Go` 매칭이 있어도 P3 반환
4. **전역 정규화 금지** — CJK 분기가 `body.split("\n")` 줄 단위. `구글_지식_그래프` 가
   `"구글 지식\n그래프"` 에 **미매칭** 확인
5. **ASCII 토큰 경계** — `(?<![a-z0-9])...(?![a-z0-9])` 가 `OS`⊂`most`/`cost`/`host` 차단.
   `[^a-z0-9]{0,3}` 구분자 허용이 `GitHub-CLI`·`GitHub (CLI)` 는 통과, `githubxcli` 는 거부
6. **frontmatter/body 분리** — P2 검출이 frontmatter 문자열만 보므로 정확

## 직접 검증 (최PM)

### 판정 함수 (구현 직후)

```
classify    AGENTS.md→ext  wikihub.yaml→ext  AST/Go/GitHub CLI→ascii  에이닷/마이크론/한자→cjk
matches     OS⊄most·cost·host ✅   AST⊄Fast·last·taste ✅   OS⊂"OS 운영체제" ✅
            Go⊂"Go language" ✅   AGENTS.md⊄"agents are useful" ✅
            구글_지식_그래프 ⊂ "구글 지식 그래프" ✅
verdict     Go(매칭無)→P3   Go(매칭有)→P3   Zebra(매칭無)→P1   Zebra(fm만)→P2   Zebra(본문有)→OK
줄범위      구글_지식_그래프 vs "구글 지식\n그래프" → False ✅ (전역 정규화 금지 동작)
```

### 실데이터 실행 (rev_match 확정 후)

```
ref_audit: total 13902 | OK 9065 | P1 1621 | P2 31 | P3 3169 | missing 16
           오염 4821건 / 315페이지
ref_ledger: total 13902 | OK 9065 | P1 1621 | P2 31 | P3 3169 | missing 16

ledger 샘플:
  {'page': 'entities/11번가.md', 'source': 'sources/nas/lecture/OpenClaw v5 강의 요약.md',
   'name_matched': '11번가', 'branch': 'cjk', 'verdict': 'OK', 'line': 75}
```

### 테스트

```
tests/test_ref_match.py   40 passed
pytest tests/ -q          161 passed, 1 skipped
기준선(v0.1.19)            121 passed, 1 skipped        ← 회귀 0, 신규 40건
```

## 독립 재현 (지사서 수치 대조)

```
지사서 3차 정정   오염 3,917건 / 313페이지
제 구현 (확정)    오염 4,821건 / 315페이지
                  ↑ P3 포함 기준. P1+P2 만 보면 1,652건
```

페이지 수(315 vs 313)가 근접해 **같은 오염 집합**을 세고 있음을 확인했습니다.

## 결론

**PASS.** [high] 는 **테스트가 검증하지 못하는 것을 검증한다고 주장한 결함**이었고,
리뷰가 이를 실제로 반증해 잡아냈습니다. 수정 후 회귀 검출력이 15배 강화됐습니다.
