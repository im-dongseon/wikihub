# Review 2 — Quality (issue #186)

- **Date**: 2026-09-21
- **Subagent**: `review-quality` (`mode: subagent`) — 정상 호출
- **Actual model**: primary 가 subagent 결과를 재검증 (아래 참조)
- **Verdict**: **Approved** (minor 2건)

## Findings and disposition

| # | Severity | Finding | 판정 | 조치 |
|---|---|---|---|---|
| 1 | minor | `install.sh` 주석 "항상 return 0 (install 중단 금지)" 가 거짓 — 실패 시 1 반환 | **타당** | 주석을 실제 계약(return 1, caller 가 graceful skip)으로 정정 |
| 2 | minor | 4.6(graceful skip) vs 4.7(`\|\| return 2`) 실패 정책 비대칭에 설명 부재 | 타당 | 4.7 에 의도(active profile config 직접 mutate, 기존 패처와 동일 정책) 주석 추가 |
| 3 | — | "heredoc delimiter 불일치 / SyntaxError" | **기각 — hallucination** | 실제 diff 재검증: `<<EOF`/`EOF` 정상 |
| 4 | — | "doc section 이 구현 함수를 참조하지 않음" | **기각 — hallucination** | `_ensure_session_env_file()`·`_patch_hermes_shell_init_files()` 둘 다 명시됨 |

## Note — 리뷰 출력 손상

subagent 출력이 PII masking(`scrubbed.*`)으로 손상되며 **존재하지 않는 결함 2건을 hallucinate**했습니다
(heredoc delimiter, doc 미참조). 둘 다 실제 diff 대조로 기각했습니다.

→ 시사점: subagent 리뷰 결과는 **반드시 실측 대조**해야 합니다. 이번 회차는 그 원칙이
실제로 잘못된 지적 2건을 걸러냈습니다.

## Confirmed correct (리뷰가 명시)

- Korean prose 문법 정상
- heading `### Hermes session env — session-env.sh (issue #186)` 위치 타당 (step 4.6/4.7 문서화, `### Feature 종료 처리` 앞)
- 비밀 분리 규칙 명확히 문서화·강제됨
