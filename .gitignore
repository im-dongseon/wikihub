# Local environment variables / config
.env
wikihub.yaml
.wikihub

# Service Account / OAuth credentials — NEVER commit (ADR-0029)
gen-lang-client-*.json
sa_*.json
token_*.json
*-sa-key.json
*-credentials.json
.credentials/

# OS-specific files
.DS_Store
Thumbs.db

# Backup / temporary
*.bak
/tmp/

# install.sh runtime sidecar (Step 3 venv path 기록) — ADR-0020·0019, 매 호출 재생성
.venv_path

# Hermes skill install artifacts (F5 — install.sh _step6_agent_skill materialized, ADR-0032)
_system/skills/_generated/

# install.sh runtime artifact — Step 4.5 가 매 호출마다 작성 (rclone/graphify/uv/wikihub 버전 fact)
_system/INSTALLED_VERSIONS.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Superpowers intermediate artifacts (optional)
docs/superpowers/

# Agent skills — deprecated / external (ADR-0046: OpenCode is canonical; Hermes is external)
.agents/
.claude/
.hermes/
skills-lock.json
